const database = require("../database/models/index");

const ProdutoService = {
  getProdutoList: async () => {},
  createProduto: async (name, description, type, price) => {},
};

module.exports = ProdutoService;
